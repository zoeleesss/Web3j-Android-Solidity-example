<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="cniao5.com.cniao5shop">

    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <!-- 蓝牙分享所需的权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- <uses-sdk tools:overrideLibrary="com.cniao5.paysdk,com.cniao5.baidupaysdk"/> -->
    <application
        android:name=".CniaoApplication"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Start"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- <activity android:name=".TestActvity"></activity> -->
        <activity android:name=".FishListActivity" />
        <activity
            android:name=".FishDetailActivity"
            android:label="@string/title_activity_ware_detail" />
        <activity
            android:name=".FishDetailActivity_center"
            android:label="@string/title_activity_ware_detail_center" />
        <activity android:name=".FishDetailActivity_center2" />
        <activity
            android:name=".FishDetailActivity_fish"
            android:label="@string/title_activity_ware_detail_fish" />

        <activity android:name=".LoginActivity" />

        <activity
            android:name=".RegActivity"
            android:label="@string/title_activity_reg" />
        <activity android:name=".History"
            android:label="@string/history_transaction"/>

        <activity android:name=".TransferActivity"
            android:label="@string/transfer"/>

        <activity android:name=".MainActivity" />



        <meta-data
            android:name="mob_sms_appKey"
            android:value="ba92fe610bb4" />
        <meta-data
            android:name="mob_sms_appSecrect"
            android:value="e73deba501fb3faaa1b5c2729e3eedbe" />

    </application>

</manifest>
